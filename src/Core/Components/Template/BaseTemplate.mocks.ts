import { IBaseTemplate } from "./BaseTemplate";

const base: IBaseTemplate = {
  name: "Base!",
};

const red: IBaseTemplate = {
  name: "Red!",
  backgroundColor: "red",
};

export const baseTemplateMockProps = {
  base,
  red,
};
